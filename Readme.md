###Docker for mongo, mongo-express and mssql-server for Linux

docker-compose run: ```docker-compose up```

docker-compose run in background: ```docker-compose up -d```

docker-compose down: ```docker-compose down```
